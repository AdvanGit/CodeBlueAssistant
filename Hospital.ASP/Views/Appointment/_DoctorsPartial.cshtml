@model IEnumerable<Entry>

@{
}

проверка удалась

    <div>
		<table class="table table-hover rounded shadow mb-0">
			<thead class="thead-dark">
				<tr>
					<th>Доктор</th>
					<th>Отделение</th>
					<th>Адрес</th>
					<th>Кабинет</th>
					<th>Дата</th>
				</tr>
			</thead>
			<tbody>
				@foreach(Entry data in Model)
				{
					<tr onclick="getDetailContent(@data.Id, false)" style="cursor:pointer">
						<th scope="row">@data.DoctorDestination.GetShortName()</th>
						<td>@data.DoctorDestination.Department.Title.Caption</td>
						<td>@data.DoctorDestination.Department.Adress.Street @data.DoctorDestination.Department.Adress.Number</td>
						<td>@data.DoctorDestination.Cabinet</td>
						<td>@data.TargetDateTime.Date.ToLongDateString()</td>
					</tr>
				}
			</tbody>
		</table>
    </div>
