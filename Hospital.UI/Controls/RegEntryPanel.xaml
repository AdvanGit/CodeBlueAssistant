<UserControl x:Class="Hospital.UI.Controls.RegEntryPanel"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Hospital.UI.Controls"
             xmlns:vm="clr-namespace:Hospital.UI.ViewModels"
             mc:Ignorable="d"
             d:DesignHeight="720" d:DesignWidth="350">
    <!--<UserControl.DataContext>
        <vm:RegistratorViewModel/>
    </UserControl.DataContext>-->

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="35"/>
            <RowDefinition Height="25"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="25"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="60"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition/>
            <ColumnDefinition/>
        </Grid.ColumnDefinitions>
        <Border Background="{DynamicResource MahApps.Brushes.Window.Background}" Grid.ColumnSpan="2" Grid.Row="0"/>
        <TextBlock Text="НАПРАВЛЕНИЕ" Grid.ColumnSpan="2" Grid.Row="0" HorizontalAlignment="Center" VerticalAlignment="Center" Foreground="Black"/>
        <TextBlock Grid.ColumnSpan="2" Grid.Row="1" Text="Врач" Style="{StaticResource tb_PanelCaptionSeparator}"/>

        <StackPanel Grid.Row="2" Orientation="Vertical">
            <TextBlock Text="{Binding SelectedStaff.FirstName}"/>
            <StackPanel Orientation="Horizontal">
                <TextBlock Text="{Binding SelectedStaff.MidName}"/>
                <TextBlock Text="{Binding SelectedStaff.LastName}"/>
            </StackPanel>
        </StackPanel>
        <TextBlock Text="Клиент" Grid.ColumnSpan="2" Grid.Row="3" Style="{StaticResource tb_PanelCaptionSeparator}"/>
        <StackPanel Grid.Row="4" Orientation="Vertical">
            <TextBlock Text="{Binding SelectedPatient.FirstName}"/>
            <StackPanel Orientation="Horizontal">
                <TextBlock Text="{Binding SelectedPatient.MidName}"/>
                <TextBlock Text="{Binding SelectedPatient.LastName}"/>
            </StackPanel>
        </StackPanel>
        <Button Content="Редактировать" Grid.Row="5" Grid.Column="0" Margin="20,15,10,15" Command="{Binding EditUser}"/>
        <Button Content="ЗАПИСЬ" Grid.Row="5" Grid.Column="1" Margin="10,15,20,15" />
    </Grid>
</UserControl>
