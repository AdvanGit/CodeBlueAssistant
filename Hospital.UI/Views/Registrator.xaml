<UserControl x:Class="Hospital.UI.Views.Registrator"
             x:Name="RegistratorView"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:mah="http://metro.mahapps.com/winfx/xaml/controls"
      xmlns:local="clr-namespace:Hospital.UI.Views"
      xmlns:controls="clr-namespace:Hospital.UI.Controls.Registrator"
      mc:Ignorable="d" 
      d:DesignHeight="600" d:DesignWidth="1280">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="90"/>
            <RowDefinition/>
        </Grid.RowDefinitions>
        <Border Grid.Row="0" Background="{DynamicResource  BrushPrimaryPanel}" Effect="{StaticResource  EffectShadowDown}"/>
        <Grid Grid.RowSpan="2" MinWidth="1080" MaxWidth="1400" HorizontalAlignment="Stretch" Margin="20,0">
            <Grid.RowDefinitions>
                <RowDefinition Height="90"/>
                <RowDefinition/>
                <RowDefinition Height="20"/>
            </Grid.RowDefinitions>

            <controls:RegSearchBar Grid.Row="0"/>
            
            <mah:TransitioningContentControl Grid.Row="1" Margin="0,20"
                                             Content="{Binding ElementName=RegistratorView, Path=CurrentControl}"/>

        </Grid>
    </Grid>
</UserControl>
